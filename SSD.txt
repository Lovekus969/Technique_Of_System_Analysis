What SSD actually represents

SSD = System Sequence Diagram
It shows interaction over time
Focuses on messages between Actor and System

Very linear (top → bottom)
  What Activity / Graph representation is

  This comes from an Activity Diagram, not SSD.
Shows workflow / control flow

Uses:

      Nodes (activities)
      Arrows (flow)
      Decisions (diamond)
      Looks like a graph
Can show:

      Conditions
      Loops
      Parallel actions

What is a System Sequence Diagram?
  A System Sequence Diagram shows:
    The actor (like Student, Customer, Admin)
    The system as a single black box
    The sequence of messages (interactions) between actor and system
    The order of events over time
